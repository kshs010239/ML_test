#include "model.h"
#include "connect.h"


int main() {
	Model<uint32_t> model;
	/*model.addLayer(FullLinearConnect(784, 256));
	model.addLayer(SigmoidConnect(256, 256));
	model.addLayer(FullLinearConnect(256, 10));
	model.addLayer(SigmoidConnect(10, 10));
	*/
	return 0;
}
